name: ExlServer-Angular POC-ExlServer

on:
  workflow_dispatch:   # Manual trigger
  push:
    branches:
      - main
    paths:
      - 'my-angular-app/**'   # Only run if files in Angular folder change
      - '.github/workflows/ci-cd-angular.yml'  # Run if workflow changes

jobs:
  build-angular:
    runs-on: [self-hosted, exlukdev]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Create build folder if not exists
        shell: powershell
        run: |
          if (!(Test-Path "E:/BuildTest/AllAppsBuild/angpoc")) {
            New-Item -ItemType Directory -Force -Path "E:/BuildTest/AllAppsBuild/angpoc"
          }

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          check-latest: true

      - name: Install dependencies
        shell: powershell
        working-directory: ./my-angular-app
        run: npm install

      - name: Build Angular App
        shell: powershell
        working-directory: ./my-angular-app
        run: npm run build -- --output-path "E:/BuildTest/AllAppsBuild/angpoc"

      - name: Verify build output
        shell: powershell
        run: |
          Write-Host "Build completed. Files in output folder:"
          Get-ChildItem -Path "E:/BuildTest/AllAppsBuild/angpoc" -Recurse
